
import { Image, StyleSheet, Text, View } from "react-native";

export default function TelaDrawer4() {
    return (
        <View style={styles.container}>
            <Text>Tela 4</Text>
        </View>
    );
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    },
});